import express from "express";

export const fileRouter = express.Router();

fileRouter.post("/upload", (req : any,res: any ) => {

})

fileRouter.get("/download/:fileId", (req: any, res: any) => {

})

